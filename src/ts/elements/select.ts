'use strict'

//import './select.scss'

//let select = document.getElementsByClassName('custom-select')
//console.log(select)

/*for (const key in select) {
    if (Object.hasOwnProperty.call(select, key)) {
        let element = select[key];
        //console.log(key, element.getElementsByClassName('dropdown-item'));

        let wrapper = document.createElement('div');
        wrapper.setAttribute('class', 'select');

        element.parentNode.insertBefore(wrapper, element);
        wrapper.appendChild(element);*/

/*element.addEventListener('click', e => {
    console.log(e.target.innerText)
})
//element.innerHTML = key
}
}*/

/*console.log(select[1].children[0])
select[0].children[0].addEventListener('click', e => {
    console.log(e.target.innerText)
})*/

/*
console.log(select[0])
select[0].addEventListener('click', e => {
    console.log(select[0], e.target)
    select[0].children[2].removeAttribute('selected')
    select[0].children[3].setAttribute('selected', '')
})

let dropdown = document.getElementsByClassName('dropdown')

let sel = document.createElement('select');

sel.style.display = 'none'

dropdown[0].appendChild(sel)*/
