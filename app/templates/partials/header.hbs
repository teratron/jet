<header class="app-header">
    <div class="app-container">
        <h1>Header</h1>
        <nav class="navbar">
            {{#each page}}
                <a href="{{this.url}}">{{this.title}}</a>
            {{/each}}
        </nav>
    </div>
</header>
